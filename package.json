{
  "name": "mcp-for-chatgpt-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "stub": "turbo run stub",
    "format": "vp fmt",
    "format:check": "vp fmt --check",
    "lint": "vp lint --type-aware",
    "lint:fix": "vp lint --fix --type-aware",
    "quality": "vp check",
    "ci": "vp check && vp run check && vp run build",
    "setup:hooks": "vp config --no-agent",
    "postinstall": "pnpm setup:hooks && turbo run stub",
    "start-mcp-server": "pnpm --filter @workspace/mcp-server start",
    "start-tunnel": "node ./scripts/tunnel-client/start-cli.js",
    "install-macos-launchd": "node ./scripts/tunnel-client/install-macos-launchd.js",
    "build-tunnel": "node ./scripts/tunnel-client/build.js",
    "test": "pnpm build && pnpm --filter @workspace/pi-adapter test && pnpm --filter @workspace/mcp-server test",
    "benchmark:tools": "pnpm build && node ./apps/mcp-server/scripts/benchmark.mjs"
  },
  "devDependencies": {
    "@types/node": "24.12.4",
    "esbuild": "0.25.12",
    "turbo": "2.10.4",
    "typescript": "6.0.3",
    "unbuild": "^3.5.0",
    "vite-plus": "0.2.4"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.10.0"
}
