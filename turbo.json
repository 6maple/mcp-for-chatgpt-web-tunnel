{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "stub": { "dependsOn": ["^stub"], "cache": false, "outputs": ["dist/**"] },
    "check": { "dependsOn": ["^check", "^build"] }
  }
}
